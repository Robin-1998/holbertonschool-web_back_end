export default class ClassRoom {
  constructor(maxStudentsSize) {
    this._maxStudentsSize = maxStudentsSize;
}
}

// constructor permet de créer une nouvelle instance de la class
// this permet d'initialiser la velur passé au constructeur
// This permet de récupérer après la variable pour l'utiliser plus tard
